mod fixed;
mod lp_pool;

fn main() {
    //    let price = Price(100);
    //    let min_fee = Percentage(1);
    //    let max_fee = Percentage(5);
    //    let liquidity_target = TokenAmount(1000);
    //
    //    let mut pool = LpPool::init(price, min_fee, max_fee, liquidity_target).unwrap();
    //
    //    let token_amount = TokenAmount(500);
    //    let lp_tokens = pool.add_liquidity(token_amount).unwrap();
    //    println!("Added liquidity: {:?}", lp_tokens);
    //
    //    let lp_token_amount = LpTokenAmount(200);
    //    let (tokens, staked_tokens) = pool.remove_liquidity(lp_token_amount).unwrap();
    //    println!("Removed liquidity: {:?}, {:?}", tokens, staked_tokens);
    //
    //    let staked_token_amount = StakedTokenAmount(100);
    //    let swapped_tokens = pool.swap(staked_token_amount).unwrap();
    //    println!("Swapped tokens: {:?}", swapped_tokens);
}
